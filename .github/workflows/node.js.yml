name: Hello World

on:
  push:
    branches:
      - main

jobs:
  saludos:
    runs-on: ubuntu-latest
    steps:
      - name: Echo the message
        run: echo "Hola mundo..."
      - name: Listar directorio
        run: ls -l